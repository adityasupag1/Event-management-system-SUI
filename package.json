{
  "name": "event-management-system-sui",
  "private": true,
  "description": "Run API + React app together for local development",
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run start --prefix backend\" \"npm run start --prefix frontend\"",
    "server": "npm run start --prefix backend",
    "client": "npm run start --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
